version: '3.8'

services:
  postgres:
    environment:
      DATABASE_URL: postgresql://degenx_user:degenx_secure_password_2024@localhost:5432/degenx

  redis:
    environment:
      REDIS_URL: redis://localhost:6379

  backend:
    environment:
      DATABASE_URL: postgresql://degenx_user:degenx_secure_password_2024@localhost:5432/degenx
      REDIS_URL: redis://localhost:6379
      POLYGON_RPC_URL: https://polygon-rpc.com
